@page
@model WarehouseClient.Pages.Resources.IndexModel
@{
    ViewData["Title"] = "Ресурсы";
}

<div class="content-wrapper">
    <div class="page-header">
        <h1>Ресурсы</h1>
    </div>

    <div style="margin-bottom: 20px;">
        <a href="/Resources/Create" class="btn btn-success">Добавить</a>
    </div>

    <table class="data-table">
        <thead>
            <tr>
                <th>Наименование</th>
                <th>Действия</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Resources)
            {
                <tr>
                    <td>@item.Name</td>
                    <td class="table-actions">
                        @if (item.Status == WarehouseClient.Models.EntityState.Active)
                        {
                            <form method="post" asp-page-handler="Archive" style="display: inline;">
                                <input type="hidden" name="id" value="@item.Id" />
                                <button type="submit" class="btn btn-warning">К архиву</button>
                            </form>
                        }
                        else
                        {
                            <span class="status-badge status-archived">Архивирован</span>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>